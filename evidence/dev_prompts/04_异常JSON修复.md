# AI 协同留痕 · 异常 JSON 修复

**问题**：部分模型返回的评分 JSON 带 Markdown 代码围栏或多余文本，解析失败。

**AI 协助**：分析日志后建议在 LLM 服务层加「容错解析」——剥离围栏、正则提取首个 JSON 对象、字段缺失兜底，并在异步队列保留失败重试。

**教师裁决**：采纳，写入 `llm_service` 解析逻辑与 `task_queue` 重试/格式校验；评分服务不可用时降级为 `_is_mock` 占位，保证学生流程不中断。
