# AI 协同留痕 · 苏格拉底式提示词调参

**目标**：让 AI 助教「不直接给答案、以追问启发」。

**迭代要点**：
- v1 问题：模型仍倾向直接给出完整代码。
- 修订：系统提示词强制「先反问学生卡点、给出最小提示、禁止输出完整可运行答案」，并加入 `ANSWER_SEEK_KEYWORDS` 识别学生「索要答案」意图后切换为引导话术。
- 验证：抽样对话确认助教以提问/小步提示为主。
